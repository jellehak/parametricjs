export default function cut({ ycad, cadData, scene, feature, object3d }) {
    const { compile, getFeatureById } = ycad
    const { selectById, sketchId, settings } = feature

    console.log("Work in progress")
        //object3d.add(mesh)
}