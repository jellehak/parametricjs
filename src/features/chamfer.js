export default {
  props: {
    entities: {
      title: 'Entities to chamfer',
      type: 'Array'
    },
    distance: {
      title: 'Distance',
      type: 'Number'
    }
  },

  mounted () {
    // return standardChamfer(context, id, definition)
    console.warn('TODO')
  }
}
